export type ThemeType = {
    yellow: string;
    grey: string;
    black: string;
};

const themeColor: ThemeType = {
    yellow: '#ffd300',
    grey: '#444444',
    black: '#000000',
};

export default themeColor;
